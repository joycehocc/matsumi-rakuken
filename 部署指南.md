# 松美洛贤教育科技 - GitHub + Cloudflare 部署指南

## 前置条件

- 已安装代理（VPN）并能访问 github.com 和 dash.cloudflare.com
- 已注册 GitHub 账号（github.com）
- 已注册 Cloudflare 账号（dash.cloudflare.com）

---

## 第一步：在 GitHub 创建仓库

1. 打开 https://github.com/new
2. Repository name 填 `matsumi-rakuken`
3. 选择 Public 或 Private（都可以）
4. **不要**勾选 "Add a README"、".gitignore"、"license"
5. 点击 Create repository

---

## 第二步：推送代码到 GitHub

在终端运行以下命令（把 `你的用户名` 改成你的 GitHub 用户名）：

```bash
cd /Users/joyceccsquirrel/.trae-cn/assistant/matsumi-rakuken-website/cloudflare-deploy

git remote add origin https://github.com/你的用户名/matsumi-rakuken.git
git push -u origin main
```

如果提示输入用户名和密码：
- Username: 你的 GitHub 用户名
- Password: 你的 GitHub Personal Access Token（不是登录密码）
  - 在 GitHub → Settings → Developer settings → Personal access tokens → Generate new token
  - 勾选 `repo` 权限
  - 复制生成的 token 作为密码输入

---

## 第三步：创建 Cloudflare KV 命名空间（替代 D1）

KV 是 Cloudflare 的键值存储，比 D1 更简单，创建过程更容易。

1. 打开 https://dash.cloudflare.com
2. 左侧菜单 → Workers & Pages → KV
3. 点击 Create a namespace
4. 名称输入 `matsumi-data`
5. 点击 Add

完成！不需要执行任何 SQL 语句。

---

## 第四步：创建 Cloudflare Pages 项目

1. Cloudflare Dashboard → Workers & Pages → Create
2. 选择 Pages → Connect to Git
3. 授权 GitHub，选择 `matsumi-rakuken` 仓库
4. 构建设置：
   - Framework preset: None
   - Build command: 留空
   - Build output directory: `public`
5. 展开 Environment Variables，添加：

   | 变量名 | 值 | 说明 |
   |--------|-----|------|
   | `ADMIN_PASSWORD` | 你的管理密码 | 如 `matsumi2026`（建议修改） |

6. 点击 Save and Deploy
7. 等待部署完成，会得到 `https://matsumi-rakuken.pages.dev` 网址

---

## 第五步：绑定 KV 命名空间

1. Cloudflare Dashboard → Workers & Pages → 你的 Pages 项目
2. Settings → Functions → KV namespace bindings
3. 点击 Add binding：
   - Variable name: `DATA`  （必须是 DATA）
   - KV namespace: 选择 `matsumi-data`
4. 保存
5. Deployments → 找到最新部署 → 点击 Retry deployment（重新部署使绑定生效）

---

## 第六步：验证部署

1. 访问 `https://matsumi-rakuken.pages.dev` — 应看到公司网站
2. 访问 `https://matsumi-rakuken.pages.dev/admin` — 应看到管理后台登录页
3. 输入管理密码登录 — 应看到留言管理页面
4. 在网站上提交一条留言 — 在后台应能看到该留言

---

## 第七步：设置自定义域名

### 如果域名在 Cloudflare 管理：

1. Pages 项目 → Custom domains → Set up a custom domain
2. 输入你的域名，例如 `www.matsumi.com`
3. Cloudflare 自动添加 CNAME 记录
4. 等待 DNS 生效（几分钟）
5. SSL 证书自动签发（免费）

### 如果域名在其他注册商：

1. 在域名注册商处添加 CNAME 记录：
   - 名称: `www`（或 `@`）
   - 目标: `matsumi-rakuken.pages.dev`
2. 在 Pages → Custom domains 中添加域名
3. Cloudflare 自动签发 SSL 证书

---

## 后续更新

每次修改代码后，只需推送到 GitHub：
```bash
cd /Users/joyceccsquirrel/.trae-cn/assistant/matsumi-rakuken-website/cloudflare-deploy
git add -A
git commit -m "Update description"
git push
```

Cloudflare 会自动检测 GitHub 更新并重新部署。

---

## 常见问题

### Q: 推送代码时提示 "Authentication failed"？
A: GitHub 不再支持密码认证，需要使用 Personal Access Token。在 GitHub → Settings → Developer settings → Personal access tokens → Generate new token，勾选 `repo` 权限。

### Q: 后台显示 "无法连接服务器"？
A: 检查 KV 命名空间绑定是否配置正确（Variable name 必须是 `DATA`），并确保已重新部署。

### Q: 自定义域名显示不安全？
A: 等待几分钟让 SSL 证书自动签发。确保 CNAME 记录正确指向 `xxx.pages.dev`。

### Q: 如何修改管理密码？
A: Cloudflare Dashboard → Pages 项目 → Settings → Environment Variables → 修改 `ADMIN_PASSWORD`，然后重新部署。

### Q: 数据存储在哪里？
A: Cloudflare KV（键值存储），免费额度：100,000 次读取/天、1,000 次写入/天、1GB 存储。
